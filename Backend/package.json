{
  "name": "backend",
  "version": "1.0.0",
  "main": "main.py",
  "scripts": {
    "start": "python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}